import React from "react";

function HomePage() {
    return (
        <>
            <h2>
                Welcome to React-Crud-Demo!
            </h2>
            <p>
                This application is a very simple crud app built using React for
                the UI and an API built using .NET Core web api. The application itself
                is a to-do list allowing for tasks to be entered into a simple list.
            </p>

        </>
    );
}

export default HomePage;